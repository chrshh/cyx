editorOpen();To implement a Leader Key (like <Space>), you need to handle "nested" input. When the Leader Key is pressed, the editor should wait for a second keypress to complete the command.To implement a Leader Key (like <Space>), you need to handle "nested" input. When the Leader Key is pressed, the editor should wait for a second keypress to complete the command.To implement a Leader Key (like <Space>), you need to handle "nested" input. When the Leader Key is pressed, the editor should wait for a second keypress to complete the command.To implement a Leader Key (like <Space>), you need to handle "nested" input. When the Leader Key is pressed, the editor should wait for a second keypress to complete the command.
editorOpen();




To implement a Leader Key (like <Space>), you need to handle "nested" input. When the Leader Key is pressed, the editor should wait for a second keypress to complete the command.To implement a Leader Key (like <Space>), you need to handle "nested" input. When the Leader Key is pressed, the editor should wait for a second keypress to complete the command.To implement a Leader Key (like <Space>), you need to handle "nested" input. When the Leader Key is pressed, the editor should wait for a second keypress to complete the command.To implement a Leader Key (like <Space>), you need to handle "nested" input. When the Leader Key is pressed, the editor should wait for a second keypress to complete the command.To implement a Leader Key (like <Space>), you need to handle "nested" input. When the Leader Key is pressed, the editor should wait for a second keypress to complete the command.

[package]
name = "asemics-rs"
version = "0.1.0"
edition = "2024"

# standalone crate: keeps the parent userland workspace from absorbing this app
[workspace]

[dependencies]
libc = "0.2"
serde_json = "1"
tree-sitter = "0.25"
tree-sitter-c = "0.24"
tree-sitter-rust = "0.24"
